# 任务 1: 默认输出目录修改验证

## 任务描述
修改 src/index.ts 第35行，将默认输出目录从 "." 改为 ".agent-harness"

## 修改内容
- 默认值: "." → ".agent-harness"
- 描述文本: "default: current directory" → "default: .agent-harness directory"

## 验证结果
执行命令: grep -n "output-dir.*default" src/index.ts

结果:
```
35: .option('-o, --output-dir <directory>', 'Output directory for generated files (default: .agent-harness directory)', '.agent-harness')
```

## 验证状态: ✅ 通过
- 默认值已更改为 .agent-harness
- 描述文本已更新为 .agent-harness directory
- 仅修改了第35行的全局选项，未影响其他代码
